const Loader = () => (
  <div className="min-h-[calc(100vh-64px)] flex items-center justify-center">
    <span className="text-xs text-stone-400 tracking-widest uppercase animate-pulse">Loading…</span>
  </div>
);

export default Loader;